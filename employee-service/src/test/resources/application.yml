server:
  client:
    key-store: client/employeeservice.jks
    key-store-password: s3cr3t
    trust-store: client/employeeservice.jks
    trust-store-password: s3cr3t
    protocol: TLSv1.2
eureka:
  client:
    enabled: false
service:
  api:
    url: https://localhost:${server.port}/api/employee
