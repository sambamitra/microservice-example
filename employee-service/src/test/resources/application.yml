server:
  client:
    key-store: server/employeeservice.jks
    key-store-password: s3cr3t
    trust-store: server/employeeservice.jks
    trust-store-password: s3cr3t
    protocol: TLSv1.2
eureka:
  client:
    enabled: false
